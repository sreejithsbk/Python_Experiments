friends = ["Kevin", "John", "Melissa"]
print(friends)
print(friends[0])
print(friends[1])
print(friends[2] + " This is awesome" "," friends[1], friends[0])

-- where -1 is  Melissa , -2 is John and -3 is Kevin when in negative
friends = ["Kevin", "John", "Melissa"]

print(friends[-1])
print(friends[-2])
print(friends[-3])

-- To display a chooses list of values out of the list, the below will only display the values starting from Index 1 and all the values 
-- following it

friends = ["Kevin", "John", "Melissa", "Tessa"]

print(friends[1:])

-- The below exaple display the index position 1 and does not display index position 3 rather it end before it.

friends = ["Kevin", "John", "Melissa", "Tessa", "Sizzle", "Tempo"]

print(friends[1:3])

-- In the following example if I change the index value at 2 "Melissa" to "Meloossa" in the second line it will make the change and print
-- out the result with the change.

friends = ["Kevin", "John", "Melissa", "Tessa", "Sizzle", "Tempo"]
friends[2] = "Meloossa"
print(friends)

--List function --EXTEND FUNCTION

lucky_numbers = [56, 23, 11, 20]
friends = ["Toosa", "Meena","Belly", "Rita"]
friends.extend(lucky_numbers)
print(friends)
